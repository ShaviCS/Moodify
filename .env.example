```bash

# Email Configuration
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Database (already configured in your app.py)
DATABASE_URL=your-database-url

# Flask Secret Key (already configured)
SECRET_KEY=your-secret-key

```
